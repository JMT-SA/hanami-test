<%= link_to 'New book', routes.new_book_path, class: 'btn' %>
<% if books.any? %>
  <% books.each do |book| %>
    <div class="book">
      <h2><%= book.title %></h2>
      <p><%= book.author %>
<%= link_to 'Edit', routes.edit_book_path(book.id) %>
</p>
    </div>
  <% end %>
<% else %>
  <p class="placeholder">There are no books yet.</p>
<% end %>
